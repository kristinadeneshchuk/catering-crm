<?php

return [

    /*
    | Через скільки днів після останньої оренди нагадати про себе.
    |
    | Дев'яносто — це осмислена пауза, а не забудькуватість: людина зробила
    | ремонт і зникла. Писати раніше означає нагадувати тому, хто й так
    | пам'ятає; писати пізніше — коли він уже орендував у конкурента.
    */
    'after_days' => (int) env('WINBACK_AFTER_DAYS', 90),

    /*
    | Не частіше ніж раз на пів року одній людині. Друге «ми скучили» підряд
    | читається як спам і коштує номера в чорному списку оператора.
    */
    'cooldown_days' => (int) env('WINBACK_COOLDOWN_DAYS', 180),

    /*
    | Скільки повідомлень за один прогін. Кожне — це гроші оператору, і база
    | на кілька тисяч клієнтів здатна вигребти місячний бюджет за хвилину.
    | Черга розбирається порціями, найдавніші — першими.
    */
    'batch' => (int) env('WINBACK_BATCH', 50),

];
