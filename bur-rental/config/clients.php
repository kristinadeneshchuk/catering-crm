<?php

use App\Services\Clients\LogCodeSender;

return [

    /*
    | Чим доставляти одноразовий код входу. Поки що — лог: SMS-шлюзу в проєкті
    | немає. Коли з'явиться, сюди підставляється клас відправника, і більше
    | ніде нічого міняти не треба.
    */
    'code_sender' => env('CLIENT_CODE_SENDER', LogCodeSender::class),

    /*
    | Показувати код прямо на сторінці. Тільки для тестового майданчика, де
    | SMS не ходять і немає доступу до логів. За замовчуванням вимкнено —
    | увімкнути це на бойовому означає віддати будь-кому чужий кабінет.
    */
    'show_code_on_screen' => (bool) env('CLIENT_SHOW_CODE', false),

];
