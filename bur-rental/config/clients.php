<?php

use App\Services\Messaging\LogSms;

return [

    /*
    | Чим слати SMS клієнту — коди входу і нагадування про повернення. Поки що
    | лог: шлюзу в проєкті немає. Коли з'явиться, сюди підставляється клас
    | відправника, і більше ніде нічого міняти не треба.
    */
    'sms' => env('SMS_DRIVER', LogSms::class),

    /*
    | Показувати код прямо на сторінці. Тільки для тестового майданчика, де
    | SMS не ходять і немає доступу до логів. За замовчуванням вимкнено —
    | увімкнути це на бойовому означає віддати будь-кому чужий кабінет.
    */
    'show_code_on_screen' => (bool) env('CLIENT_SHOW_CODE', false),

];
